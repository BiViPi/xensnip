import type { FrameFamily } from '../types';

interface Props {
  value: FrameFamily;
  onChange: (f: FrameFamily) => void;
}

export function FrameTypePicker(_props: Props) {
  // TODO WS5
  return null;
}
