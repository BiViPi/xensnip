import type { FrameStyle } from '../types';

interface Props {
  value: FrameStyle;
  onChange: (s: FrameStyle) => void;
}

export function FrameStylePicker(_props: Props) {
  // TODO WS5
  return null;
}
